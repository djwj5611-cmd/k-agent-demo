# K-Agent — Demo & Trust Surface

> 공개 데모 전용 리포지토리입니다. 제품 소스 코드는 포함하지 않습니다.
> 모든 예제는 **합성(synthetic) 데이터**이며 실제 고객 데이터가 아닙니다.

## 무엇을 보여주나

한국어 tool-using AI 에이전트가 "답변은 그럴듯했지만 실제 비즈니스 상태를 잘못 바꾼" 경우를
K-Agent의 결정론적 평가기가 잡아내고, 수정 후 재실행(rerun)에서 통과하는 과정입니다.

- `index.html` — 3분 읽기 신뢰 페이지 (실패 예시 → 판정 → 수정 → PASS)
- `trace-failure.json` — 합성 실패 trace (부분 취소 요청에 전체 취소 실행)
- `trace-fixed.json` — 합성 수정 trace (조건부 부분 취소, REFUNDED 정상 종료)

## 제공 서비스 (고정 범위)

**마이크로 Agent 신뢰성 진단** — workflow 1개, 고객 승인 trace 약 20건, 48–72시간,
실패 Top-5 + 재실행 가능한 프라이빗 회귀 팩 + 수정 rerun 1회.

가격은 문의 시 안내합니다. 고객 데이터는 학습·타고객 재사용·재배포 모두 불허(기본값)이며
처리는 로컬/고정 버전에서 수행하고 30일 내 삭제 영수증을 발행합니다.

## 문의

이 페이지의 contact 링크를 통해 연락 바랍니다.
